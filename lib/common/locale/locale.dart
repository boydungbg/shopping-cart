import 'package:flutter/widgets.dart';

class LocaleConfigs {
  static const Locale vi = Locale('vi');
  static const Locale en = Locale('en');
  static const Locale defaultLocale = vi;

  static const List<Locale> supportedLocale = [vi, en];
}